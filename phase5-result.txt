Phase 5 — Lead scoring & escalation
===================================
Status: PASS (with one data-state note)

Checked:
  1. leads.score consistency with signals per lib/scoring.ts weights
     (email 25, phone 15, name 10, service_interest 15, urgency 20,
     sustained_conversation 10 for 4+ visitor messages).
  2. detectUrgency() on real recorded visitor messages.
  3. Escalation state: status='escalated' leads have a matching
     conversation with status='escalated' + non-empty summary_text.

Found:
  - One lead (7e5361d9) had score=0 despite 5 visitor messages in a single
    conversation. Reproduced the exact recalculateLeadScore computation and
    persisted it: the score updated to 10 (the expected sustained-conversation
    bonus). Root cause: the lead was test data inserted without the widget
    route running, so recalculateLeadScore had never executed for it. This is
    stale data, NOT a code bug. The scoring function and persistence path work.

Escalation:
  - Lead d2f99285 (Jane Doe, score=50) has a conversation a4fc3684 with
    status='escalated' and a non-empty summary_text:
      "Jane Doe wants to book an appointment for August 4, 2026, ..."
  - This is the intended escalation record: lead escalated + conversation
    escalated + summary_text populated. Matches the runbook requirement.

Fixed:
  - Re-ran the scoring persistence for the stale lead (score 0 -> 10), so the
    leads pipeline now shows a consistent score. No source code change was
    needed because lib/scoring.ts is correct; the stale value came from data
    that bypassed the widget route. (Also preserved on-disk phase5-result for
    the audit trail.)

Remaining:
  - None. detectUrgency flags real urgently-worded messages; escalation state
    is correct. Noting that all leads with score=0 are anonymous 1-2 message
    visitors, which is expected.
